const imgCertificado = document.querySelector('.platzi-imagenes')

const imgControl = () =>{
    if(imgCertificado.classList[a[img[hover]]]){
        imgCertificado.classList.add('imagenCertificado')
    }else{
        imgCertificado.classList.remove('imagenCertificado')
    }
}

imgControl()